# Todo 


* Project API refactor: Some properties of project is manipulated directly via UI binds for example the name of each layer, make a clean and good API 
for updating the project state but make sure that the properties are 
still reactive according to svelte, review Error handling, how will GUI or other clients receive errors, is it via throws? 
Most errors are "not found", "type mismatch between layer and data" and "asset in use",
 API för projectstate tydligare att "areas" är en modul och inte listan av areas 

Errors: Gör koder i enums istället för 

* Flood fill

* Save As .ptm 

* Load .ptm

* Auto save project with browser data indexed DB or localStorage

* Use proto files for binary file format 

* Reactivity issue with layers and dnd action, items loose reactivity when it comes to classes/styling and become unable to interact with

* Adjust canvas scale to dpr to avoid blurry lines

# Backlog

* Add a image next to the cursor to show selected asset that the user is painting with -> not necessary, adds clutter I think 

* Generate maze 

* Add animated tiles which consists of 1-4 tiles that are running an animation instead of a static image

