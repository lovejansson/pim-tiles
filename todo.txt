# Todo 

5. Save state in indexed DB

1. Gillar inte hur templat hanteringne med PaintType har fungerat, det ger inte det typstöd jag vill ha 

2. Make auto tiling faster by using bitmask for lookup a tilerule than looping throught the rules.

3. pass by reference / copy, right now t


# Backlog

* Generate maze tool 

* Add animated tiles which consists of 1-4 tiles that are running an animation instead of a static image

* Implement fill tool for auto tiles

* Use deviceDependedpixel size to avoid blur for grid lines, not very noticable

* Svelte reactivity for conditional attributes don't work with dnd reordering of layers. There are workarounds in place now. 
