# Todo

Before GB town can be done 

* Add attribute for a Tile 

// right click on tile to add attributes for it? is not layer dependent

// right click for tilemap 
// check row console
// display edit tile attributes 
// in edit tile attributes -> row col, add or remove tile attributes for that tile 
// if tile attributes don't exist -> create a tile attribute entry
// if this gets saved it is saved to the attributes map 
// i app vill jag kunna vad? se att en viss dör är på ett sätlle säger vi, "här är någons hem", 
attribute blir "home", value blir "oskar"

in editor attributes => Map<string, attributes>
exported => [{x: numer, y: number, attributes: [key: string]: any}]

så i js programmet måste jag göra en find

* weird to keep CRUD API for projectstate when GUI updates it by updating the objects directly, for example layer name

* Select tool ->
    Move, 
    Delete,
    Copy and paste 



FILE 

* Implement Save

* Implement Save AS


After

* Errors from updates of project state, don't want to throw in the console

* Flood fill

* Pan when cursor moves out of view (useful if you want to move an object far away)

* Add a image next to the cursor to show selected asset that the user is painting with -> not necessary, adds clutter I think

* Generate maze

* Randomize placement of something within an area like trees

* Add animated tiles which consists of 1-4 tiles that are running an animation instead of a static image
