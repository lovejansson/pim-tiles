# Todo

* Add flood fill for auto tile 

* Change tile paint to not be a list of tiles but a grid of the tileset that the user can select multiple tiles from to paints with
 
FILE 

* Implement Save
* Implement Save AS
* Implement export of JSON 
* Implement sync with local storage


* Maybe add extras for generating a maze and other pattern like stuff

* Pan add drag of tilemap

* Decide how to add attributes to objects and tiles, what is the need here, sprite fusion has attributes per tile and per tile instance

* Errors from updates of project state, don't want to throw in the console
