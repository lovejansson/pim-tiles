# Todo 

# Backlog

* Maze tool 

* Animated tiles

* Use deviceDependedpixel size to avoid blur for grid lines, not very noticable

# Bugs 

* Sometimes a tile is left on the tilemap even if it doesn't exist in data, probably due to cache miss? Kan bero på att jag målar på fel lager också lol

* Sometimes there is a cache miss for layer when drawing probably due to cache miss again, meaning that the cache doesn't update when layers do

* Sometimes the undo command hangs

* Flood fill to slow for large auto tile paint areas

* Svelte reactivity for conditional attributes don't work with dnd reordering of layers. There are workarounds in place now.