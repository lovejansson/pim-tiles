# Todo 

* Create object layer

* Build GB town 

# Bugs 

# Improvements 

* Sync of changes in indexed db between tabs

* Auto tiles finns kvar även om alla auto tile lager är borta, oklart om det ska vara så

* Selection for bigger areas is a bit slow and selection contains some bugs about grid bounds. Problems that arise when testing edge cases like moving and copying bigger areas 

* make it possible to change dimensions without wiping the canvas, place current content inside of new dimensions 
and choose where to start drawing the old content, 

* Lägg till feature för enkla aritmetiska beräkningar i inputs, / * + och - 

* To fill areas with rect instead of flood fill 

* Replace the DND kit if it doesn't work, has problems with reactivity UI on several places like the eye icon not working at all
---------> chunk-YUTDRR6G.js?v=db1b4a4a:4872 Uncaught TypeError: Cannot set properties of undefined (setting 'name')
    at toggleVisibility (LayerItem.svelte:56:16)
    at LayerItem.sl_icon_button_1.__click (LayerItem.svelte:94:9)


# Future backlog

* Maze tool 

* Animated tiles

* Use deviceDependedpixel size to avoid blur for grid lines, not very noticable

* Use pixie JS for rendering